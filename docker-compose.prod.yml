services:
  app:
    image: zstin4/web-portfolio:${IMAGE_TAG}
    container_name: web_portfolio
    restart: always
    expose:
      - "80"
    networks:
      - web

networks:
  web:
    external: true
    name: web