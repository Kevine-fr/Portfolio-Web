services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: web_portfolio
    restart: always
    expose:
      - "80"
    networks:
      - backend

networks:
  backend:
    external: true
    name: backend