services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: web_portfolio
    restart: always
    expose:
      - "80"
    networks:
      - web

networks:
  web:
    external: true
    name: web